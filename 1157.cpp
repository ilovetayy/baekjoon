#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main(){
    string input;
    vector<char> str;
    vector<int> cnt;
    int i;

    cin >> input;

    for(i=0; i<input.size(); i++){
        //if(find(str.begin(), str.end(), '') != str.end()){//contains

        //}
    }
    return 0;
}